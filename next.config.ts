import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    images: {
        domains: ["m.media-amazon.com", "stat5.bollywoodhungama.in", "upload.wikimedia.org", "encrypted-tbn0.gstatic.com"],
    },
};

export default nextConfig;
